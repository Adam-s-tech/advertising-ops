# Output Paths (memory)

Saved once, reused every run. The pipeline checks this file before asking where to put files. If a path is already set below, reuse it without re-asking (a quick "still putting reports/creatives here?" confirm is fine). If a path is still `[Not yet set]`, ask the user once, then write their answer back into this file so every future run reuses it automatically.

## Report path
[Not yet set]

## Creatives path
[Not yet set]

## Notes
- Last updated: [Not yet set]
